import { ReactElement } from 'react';
import './App.css';
import SunTimes from './SunTimes';

function App(): ReactElement {
    return (
        <div className='App'>
            <SunTimes />
        </div>
    );
}

export default App;
